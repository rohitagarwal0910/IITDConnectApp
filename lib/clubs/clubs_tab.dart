import 'package:flutter/material.dart';

class ClubsTab extends StatelessWidget {
  final String title = 'Clubs';

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.all(25),
      child: Text('Club List Here'),
    );
  }
}
